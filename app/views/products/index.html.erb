<h1>All Products</h1>

<div>
    <table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Brand</th>
            <th>Category</th>
        </tr>
    </thead>

    <tbody>
        <% @products.each do |product| %>
            <tr>
                <td><%= link_to product.name, product_path(product) %></td>
                <td><%= product.brand %></td>
                <td><%= product.category.name %></td>
            </tr>
        <% end %>
    </tbody>
</table>
</div>